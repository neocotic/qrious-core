/* eslint-disable no-console */
import { renderText } from '../src/index';

// A simple example of rendering text.
console.log(renderText({ value: 'https://example.com' }));

// You can also do:
console.log(renderText('https://example.com'));
